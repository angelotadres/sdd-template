# Tech Stack

<!--
Purpose: Document *how* the system is built and why those choices were made.
This file changes when core architecture or dependencies change. Each decision
should be justified in a sentence — future-you and future-agents will thank you.
-->

## System Design

<!-- One or two paragraphs describing the high-level architecture. What are the major components and how do they talk to each other? Use a Mermaid diagram (```mermaid block) if a visual helps — it renders natively on GitHub. -->

## Stack Choices

<!-- Language, framework, database, key libraries. Each with a one-line rationale. Example:

- **Language:** <name> — <reason>.
- **Framework:** <name> — <reason>.
- **Database:** <name> — <reason>.
- **LLM provider:** <name> — <reason>.
-->

## Data Model Overview

<!-- The main entities and their relationships. Not a full schema — that belongs in code. Just enough that an agent knows the shape. -->

## Interfaces

<!-- API surface, CLI commands, or UI surfaces. What are the entry points into the system? -->

## Configuration

<!-- Environment variables and their purposes. Include defaults. -->

## Testing Strategy

<!-- How do you know the code is correct? What's tested automatically vs. manually? What's the smoke-test path? -->

## Dependencies

<!-- Core runtime dependencies (not every transitive one). Flag anything unusual or risky. -->

## Open Questions

<!-- Things you haven't decided yet. Tracking them here prevents them from being quietly re-decided by whoever codes first. -->
